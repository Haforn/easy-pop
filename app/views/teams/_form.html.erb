<ul>
	<%= form_for @team do |f| %>
		<li>
			<%= f.label :title, 'Team naam' %>
			<%= f.text_field :title, placeholder: 'Geef je team een naam!' %>
		</li>

		<li>
			<%= f.submit nil, class: 'btn new bigger left' %>
		</li>
	<% end %>
</ul>